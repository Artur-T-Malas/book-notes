<%- include("partials/header.ejs") %>
<main>
    <h2>Register</h2>
    <p>
        <a href="/">Back to the main page</a>
    </p>
    <p>
        <span>
            <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="#dc3545">
                <path d="M480-280q17 0 28.5-11.5T520-320q0-17-11.5-28.5T480-360q-17 0-28.5 11.5T440-320q0 17 11.5 28.5T480-280Zm-40-160h80v-240h-80v240Zm40 360q-83 0-156-31.5T197-197q-54-54-85.5-127T80-480q0-83 31.5-156T197-763q54-54 127-85.5T480-880q83 0 156 31.5T763-763q54 54 85.5 127T880-480q0 83-31.5 156T763-197q-54 54-127 85.5T480-80Zm0-80q134 0 227-93t93-227q0-134-93-227t-227-93q-134 0-227 93t-93 227q0 134 93 227t227 93Zm0-320Z"/>
            </svg>
        </span>
        This register form is not secure. It is part of a project made for learning purposes.
    </p>
    <p><strong>Don't use any "real" password you have here. Don't reuse passwords used here for any other purpose. Consider any passwords used here to be compromised.</strong></p>
    <div id="register-panel">
        <div class="form-container">
            <% if (locals.isRegisterFailed) { %>
                <% if (locals.alreadyExists && locals.username) { %>
                    <p class="text-warning">User <strong><%= locals.username %></strong> already exists. Did you mean to <a href="/login">Login</a> instead?</p>
                <% } else if (locals.isEmailAlreadyUsed && locals.email) { %>
                    <p class="text-warning">Email address <strong><%= locals.email %></strong> is already in use. Try to <a href="/login">Login</a> or create an account with a different email address.</p>
                <% } else { %>
                    <p class="text-danger">An error has occurred. Please contact the administrator.</p>
                <% } %>
            <% } %>
            <form id="login-form" class="login-form" action="/register" method="post">
                <label for="username-input">Username</label>
                <input type="text" name="username" id="username-input" minlength="3" maxlength="20" pattern="[a-zA-Z0-9]{3,20}" title="Username must be 3-20 characters long, letters and numbers only" required>
                <label for="email-input">Email Address</label>
                <input type="email" name="email" id="email-input" maxlength="100" required>
                <label for="password-input">Password</label>
                <input type="password" name="password" id="password-input" minlength="6" maxlength="30" pattern="[a-zA-Z0-9\-_#$@]{6,30}" title="Password must be 6-30 characters long: letters, numbers, and - _ # $ @" required>
                <button type="submit" class="button-primary" id="register-btn" disabled>Register</button>
            </form>
        </div>
        <p>Already have an account? <a href="/login">Login</a></p>
    </div>
</main>
<script type="text/javascript" src="../scripts/register.js"></script>
<%- include("partials/footer.ejs") %>